---
# tasks file for ansible-jenkins
- include: debian.yml
  when: install_jenkins is defined and install_jenkins

- include: users.yml
  tags:
    - configure_jenkins_user
  when: install_jenkins is defined and install_jenkins

- include: plugins.yml

- include: config_jenkins.yml
  when: config_jenkins is defined and config_jenkins
